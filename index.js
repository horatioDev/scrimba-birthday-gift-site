const btn = document.querySelector('.generate-btn');
const gifContainer = document.querySelector('.gift');

btn.addEventListener('click', generateGif);

function generateGif() {
  alert('Button clicked! CURRENTLY DISABLED!')
}